statement: |2+

  `while``for`を使って、1から3までをint(input())で入力した回数出力してください。<br>
  出力する時は、すべて元の数を２を割ってください。


testcase:
  tests:
  - input:
    - "3"
    output: |
      0.5
      1.0
      1.5
      0.5
      1.0
      1.5
      0.5
      1.0
      1.5
code: |
  count = 0
  b = int(input())
  while True:
    for i in range(1,4):
      print(i / 2)
    count += 1
    if count == b:
      break
